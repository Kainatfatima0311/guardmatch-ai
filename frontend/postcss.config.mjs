// Tailwind v4 is a PostCSS plugin and configures itself from CSS (see
// src/app/globals.css) rather than from a JavaScript config file.
const config = {
  plugins: { "@tailwindcss/postcss": {} },
};

export default config;
